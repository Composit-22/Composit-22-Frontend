import classes from "./ComingSoon.module.css"

const ComingSoon = () => {
    return (
        <section className={classes["coming-soon"]}>
            <h1 className={classes["coming-soon__heading"]}>Coming Soon</h1>
        </section>
    );
};

export default ComingSoon;